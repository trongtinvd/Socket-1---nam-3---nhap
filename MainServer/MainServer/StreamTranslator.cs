using System;
using System.Net.Sockets;

namespace MainServer
{
    internal static class StreamTranslator
    {
        internal static string Read(NetworkStream stream)
        {
            return "";
        }
    }
}